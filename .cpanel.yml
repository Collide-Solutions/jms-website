---
deployment:
  tasks:
    - export DEPLOYPATH=/home1/jmsuniversal/repositories/jms-website
    - /bin/bash -c "source /home1/jmsuniversal/nodevenv/repositories/jms-website/20/bin/activate && cd $DEPLOYPATH && npm install && npm run build"
